import { createUseStyles } from 'react-jss';

export default createUseStyles({
	header: {
		display: 'flex',
		alignItems: 'baseline',
		margin: [36, 0, 0],
	},

	date: {
		margin: [0, 0, 0, 12],
		color: '#aaa',
		fontSize: 14,
		fontStyle: 'italic',
	},
});
